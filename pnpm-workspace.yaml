packages:
    - 'packages/*'

# Required by pnpm 10's deploy command so the desktop runtime can package the
# built workspace dependencies without re-resolving the entire public registry.
injectWorkspacePackages: true
